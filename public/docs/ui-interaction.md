# UI Interaction & Platform Management

The Zeus Platform provides a powerful, highly optimized web application that puts you in full control of your infrastructure's behavioral analysis, dataset collection, and machine learning pipelines. This guide covers the major interfaces and workflows within the Admin Dashboard.

> [!NOTE]
> The screenshots below are placeholders and will be populated with actual UI captures as the dashboard evolves.

## 1. Dashboard (Network Overview)
![Dashboard Overview](/public/images/ui/dashboard.webp)

The Dashboard provides real-time situational awareness of your connected servers and player base. It is the primary landing view for monitoring network-wide enforcement.

* **Live Network Activity**: View the total count of connected backend servers and active players across your network. You can search, paginate, and monitor individual servers and players.
* **Violating Players**: Displays a list of players flagged for high severity anomalies. You can visually toggle alert sounds and flashes, dismiss specific alerts, or permanently delete all violation history.
* **Integrations API**: Features a quick-reference snippet for an Integrations API endpoint for programmatic retrieval or clearing of violation records.

---

## 2. System Health & Control
![System Health](/public/images/ui/system.webp)

Monitor server infrastructure resources and manage server node instances efficiently to prevent memory leaks and ensure peak performance.

* **RAM Usage Tracking**: A real-time gauge displaying the memory footprint utilized by the platform in KB/MB/GB.
* **Memory Purging (`Free All Memory`)**: Allows administrators to forcefully dump in-memory datasets, player registries, and adaptive engine histories to reclaim RAM space after heavy training tasks or extended uptimes.
* **Server Control**: Remotely Start or Stop the core telemetry server directly from the UI without accessing the command line.

---

## 3. ML Training Center
![ML Center](/public/images/ui/ml_center.webp)

This module empowers administrators to manage the lifecycle of machine learning models for anomaly detection without leaving the browser. It integrates feature extraction, local training, and dataset curation.

* **Runtime Active Models**: View the models actively running in production and interpreting live telemetry.
* **Train New Model**: Easily configure training hyperparameters tailored to your dataset:
    * **Profile Types**: Train models specifically for Movement, Combat, Interact, Transaction, or Networking.
    * **Model Tuning / Continuous Learning**: Toggle this technique to help models continually learn new capabilities without catastrophic forgetting.
    * **Dataset Clean-up**: Automatically purges noisy samples before training.
* **Export Dataset**: Download curated subsets of collected data directly to your server's local filesystem as raw CSVs.
* **Feature Collector**: The engine that drives continuous data collection. Toggle between `Full` mode (for maximum data gathering) and `Limited` mode (a ring-buffer that prevents memory exhaustion). Adjust disconnect policies to either `Clean` or `Retain` data upon player logout.

---

## 4. Data Analysis
![Data Analysis](/public/images/ui/data_analysis.webp)

A deep-dive tool intended for visually analyzing downloaded CSV datasets or programmatically merging structural `.json` distributions.

* **Distribution Fusion (Ultra)**: Upload multiple structural `distributions.json` files to merge them algorithmically. The system calculates the average `μ` (mu) and `σ` (sigma) to build an "ultimate" consolidated profile, heavily reducing false positives.
* **CSV Data Preview**: Load gigabytes of raw behavioral data into a memory-safe browser data table, with options for dynamic pagination.
* **Anomaly Visualizations**: Automatically generated insight charts visualizing Score Distributions, Temporal Sample Trends, Feature Means, Variance distributions, and Feature Correlation Matrices.

---

## 5. System Console
![System Console](/public/images/ui/console.webp)

The built-in console offers a low-latency web-based terminal that mirrors the backend process output.
* Monitor standard output and standard error streams in real-time.
* View exact internal logging generated by the inference engine and networking threads.

---

## 6. Global Configuration
![Configuration](/public/images/ui/configuration.webp)

The central settings hub handles all internal configurations, from cluster networking to precise parameter overriding. For a detailed breakdown of the options provided in the Configuration tab, refer to the dedicated [Configuration Guide](/docs/configuration).

---

## 7. Replay System (Replay & Evidence Review)
![Replay System](/public/images/ui/replay.webp)

The Replay System provides operators with the ability to visually review player activity and verify suspicious behavior without exposing private detection mechanics. By recording high-fidelity telemetry, it reconstructs a visual representation of anomalies, giving administrators clear, irrefutable evidence before taking enforcement action.

* **Visual Replay Viewer**: Step back and forth through a frame-by-frame 3D reconstruction of player movements, actions, and orientation leading up to and during a flagged anomaly.
* **Telemetry Insights**: Inspect concurrent telemetry graphs mapping speed, acceleration, vector deviation, and input rates directly alongside the visual representation.
* **Evidence Management**: Export localized replay bundles containing raw telemetry and metadata packages. These packages can be shared among administrators for peer-review or archived as historical reference.
* **Privacy-Safe Design**: Replay telemetry focuses purely on physics and spatial coordinates. It never records personal identifier data (PII) or internal server heuristics, keeping the review process compliant with security standards.
